# Roblox / Rojo build artifacts
*.rbxl
*.rbxlx
*.rbxl.lock
*.rbxlx.lock
/build/
build.rbxlx
build.rbxl

# Locally installed toolchain binaries (teammates install via rokit; see rokit.toml)
/bin/
.rokit/

# Lune
.lune/

# Selene generated Roblox std (regenerate with `selene generate-roblox-std`)
roblox.yml

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.log

# Node (only present if local tooling needs it)
node_modules/

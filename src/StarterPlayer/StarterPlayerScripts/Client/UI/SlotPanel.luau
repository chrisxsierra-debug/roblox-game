--!strict
-- SlotPanel — contextual panel for a tapped slot (§19). TRACK B. STUB.
-- Empty -> hand off to SeedPicker. Growing -> silhouette + progress + remaining +
-- stage + optional fertilizer. Ready -> full creature + mutation + estimated payout
-- (informational only) + large Harvest button + "READY!".

local SlotPanel = {}

function SlotPanel.create(_parent: Instance): { [string]: any }
	return {}
end

return SlotPanel

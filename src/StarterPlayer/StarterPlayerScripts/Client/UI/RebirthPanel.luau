--!strict
-- RebirthPanel — rebirth confirmation dialog (§10, §19). TRACK B. STUB.
-- Shows the server-computed requirement and what resets vs persists; requires an
-- explicit confirm before firing RequestRebirth.

local RebirthPanel = {}

function RebirthPanel.create(_parent: Instance): { [string]: any }
	return {}
end

return RebirthPanel

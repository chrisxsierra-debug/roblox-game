--!strict
-- SeedPicker — choose which unlocked seed tier to plant (§19). TRACK B. STUB.
-- Shows each tier's cost, grow time, base payout, and lock requirement (reads
-- SeedTierDefinitions + EconomyConfig for DISPLAY only).

local SeedPicker = {}

function SeedPicker.create(_parent: Instance): { [string]: any }
	return {}
end

return SeedPicker

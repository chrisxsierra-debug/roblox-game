--!strict
-- NotificationPanel — transient toasts + milestone messages (§25). TRACK B. STUB.
-- Renders Notification events (mutation reveals, milestones, "your crops kept
-- growing" return message). Non-blocking, auto-dismiss.

local NotificationPanel = {}

function NotificationPanel.create(_parent: Instance): { [string]: any }
	return {}
end

return NotificationPanel
